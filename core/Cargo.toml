[package]
name = "thumbnailer-core"
version = "0.1.0"
edition = "2024"

[dependencies]
image     = { version = "0.25", features = ["webp"] }
ab_glyph  = "0.2"
serde     = { version = "1", features = ["derive"] }
serde_json = "1"
